service: jobzshala-gcc-backend

provider:

  name: aws

  runtime: nodejs20.x

  region: ap-south-1

functions:

  api:

    handler: dist/lambda/api.lambda.handler

    timeout: 30

    memorySize: 512

    events:

      - httpApi: '*'